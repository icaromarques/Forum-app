
import {bindable} from 'aurelia-framework';

export class WindowHeaderCustomElement{
  @bindable title;
  @bindable subtitle;
  @bindable subtitleHint;
  @bindable showClose;

  constructor(){

  }}
